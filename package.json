{
  "name": "@lexmata/nestjs-platform-bun",
  "type": "module",
  "version": "0.3.0",
  "description": "NestJS HTTP adapter for Bun runtime",
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.cjs",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    },
    "./package.json": "./package.json"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "files": [
    "dist",
    "src",
    "!src/**/*.test.ts"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "test:coverage": "bun test --coverage",
    "test:unit": "bun test src/",
    "test:e2e": "bun test test/e2e/",
    "typecheck": "tsc --noEmit",
    "_comment:typecheck:test": "Separate from `typecheck` because the root tsconfig only includes src/. test/e2e currently has 48 type errors (see tsconfig.test.json); this gate tracks them without turning the main gate red. Fold it back into `typecheck` once test/ is clean.",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "typecheck:bench": "cd benchmark && pnpm typecheck",
    "typecheck:examples": "pnpm build && cd examples && pnpm install && pnpm typecheck",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "bench": "pnpm build && cd benchmark && pnpm install && pnpm bench",
    "bench:verify": "pnpm build && cd benchmark && pnpm install && pnpm verify",
    "prepublishOnly": "pnpm build",
    "prepare": "husky",
    "_comment:deprecate:old": "Run ONCE by a maintainer, by hand, after `npm publish` of the first @lexmata release succeeds. Points anyone still installing the old @pegasusheavy name at the new one. Requires npm publish rights on the @pegasusheavy scope. Not wired into any other script and never run by CI.",
    "deprecate:old": "npm deprecate \"@pegasusheavy/nestjs-platform-bun@*\" \"Renamed to @lexmata/nestjs-platform-bun\""
  },
  "keywords": [
    "nestjs",
    "bun",
    "http",
    "adapter",
    "platform"
  ],
  "author": "Lexmata",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Lexmata/nestjs-bun.git"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0 || ^11.0.0",
    "@nestjs/core": "^10.0.0 || ^11.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.3.0",
    "@commitlint/config-conventional": "^20.3.0",
    "@eslint/js": "^9.18.0",
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@types/bun": "^1.2.4",
    "eslint": "^9.18.0",
    "husky": "^9.1.7",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "tsup": "^8.4.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.21.0",
    "vitest": "^4.0.16"
  },
  "packageManager": "pnpm@10.26.2"
}
